export default function App() {
  return (
    <div style={{ fontFamily: "sans-serif", padding: 40 }}>
      <h1>Scout — scaffold placeholder</h1>
      <p>If you can read this in the browser, the Vite + React setup works.</p>
      <p>Next step: swap in the real Scout prototype UI.</p>
    </div>
  );
}
